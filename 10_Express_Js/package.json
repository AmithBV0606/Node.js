{
  "name": "10_express_js",
  "version": "1.0.0",
  "description": "```javascript\r const http = require(\"http\");\r const fs = require(\"fs\");\r const url = require(\"url\");",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
