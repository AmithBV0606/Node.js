const math = require("./math");
const fs = require("fs"); // In-built library/module
// const {sum, sub} = require("./math");

console.log(math);
console.log(math.sub(50, 30));
// console.log(sum(50, 30));
console.log(fs);